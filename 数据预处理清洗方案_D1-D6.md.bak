# 数据预处理清洗方案（D1-D6）

> 本文档定义从原始数据到 clean 单表的完整清洗流程。每个数据集输出一张单表 CSV，供下游分离脚本直接消费。
>
> 生成日期：2026-06-18

---

## 0. 通用约定

### 0.1 输出规范

每个数据集清洗后输出一张 CSV，命名为 `Dx_clean.csv`，统一存放到：

```
outputs/preprocessing/
├── D1_clean.csv
├── D2_clean.csv
├── D3_clean.csv
├── D4_clean.csv
├── D5_clean.csv
└── D6_clean.csv
```

### 0.2 统一要求

所有数据集的 clean 表必须满足以下条件：

- 日期列统一命名为 `date`，格式 `YYYY-MM-DD`，dtype 为 string（CSV 不保存 datetime）
- 目标列统一命名为 `sales`（原始列名在清洗中重命名）
- 实体标识列保留原始命名（`store_id`、`item_id` 等），不做统一化
- 所有 datetime 衍生特征（year、month、week、day）在此步骤提取
- 不做 min-max 归一化（归一化放到实验 pipeline 中，因为 scaler 必须在 train set 上 fit）
- 不做时间窗口裁剪（全量历史保留，裁剪放到下游分离/实验步骤）
- 不做实体分离（target/source 拆分是下一步的事）

### 0.3 Datetime 特征提取规范

所有 6 个数据集统一提取以下 4 个特征（对齐论文 Fig. 9）：

```python
df['year'] = df['date'].dt.year
df['month'] = df['date'].dt.month
df['week'] = df['date'].dt.isocalendar().week.astype(int)
df['day'] = df['date'].dt.day
```

如果数据集本身有额外特征（促销、天气等），保留并清洗，不删除。

### 0.4 大文件处理

- 所有 >500MB 文件必须用 chunked 读取（`chunksize=500000`）或 dask
- D4 的 list 类型列需特殊解析
- D6 宽表转长表时注意内存峰值

---

## 1. D1 — Store Item Demand Challenge

### 1.1 数据源

```
路径：<你自己粘贴>
原始文件：Dataset1-Challenge.csv
```

### 1.2 原始结构

| 字段 | 类型 | 说明 |
|------|------|------|
| date | string | 日期 |
| store | int | 门店 ID（1-10） |
| item | int | 商品 ID（1-50） |
| sales | int | 日销量 |

实体数：500（10 stores × 50 items），时间跨度：2013-01-01 ~ 2017-12-31（5 年）。

### 1.3 清洗步骤

1. **读取**：`pd.read_csv(path, parse_dates=['date'])`
2. **列重命名**：`store` → `store_id`，`item` → `item_id`（保持与 D4-D6 风格一致）
3. **数据质量检查**：
   - 确认无缺失值、无负值、无重复行
   - 确认实体数 = 500，日期连续无断档
4. **Datetime 特征提取**：year、month、week、day
5. **输出列顺序**：`date, store_id, item_id, sales, year, month, week, day`

### 1.4 预期产物

- 行数：约 913,000（500 实体 × ~1,826 天）
- 列数：8
- 无特殊清洗操作，D1 数据质量最干净

### 1.5 验证检查项

- [ ] `df.isnull().sum()` 全部为 0
- [ ] `df['sales'].min() >= 0`
- [ ] `df.groupby(['store_id','item_id']).ngroups == 500`
- [ ] datetime 特征值域合理（year ∈ [2013,2017]，month ∈ [1,12] 等）

---

## 2. D2 — Pasta Dataset

### 2.1 数据源

```
路径：<你自己粘贴>
原始文件：Dataset2-pasta.csv
```

### 2.2 原始结构

| 字段 | 类型 | 说明 |
|------|------|------|
| date | string | 日期 |
| brand/item 相关列 | int | 品牌+商品标识 |
| sales | float | 日销量 |
| promotion | binary | 促销标识 |

实体数：~118，时间跨度：2014-01-01 ~ 2018-12-31。

### 2.3 清洗步骤

1. **读取**：`pd.read_csv(path, parse_dates=['date'])`
2. **列重命名**：将品牌/商品列统一为 `brand_id`、`item_id`；销量列确认为 `sales`
3. **保留 promotion 列**：作为额外特征（对齐论文 Fig. 9，D2 有 promotion 特征）
4. **数据质量检查**：
   - 确认无缺失值、无负值
   - 确认实体数 ~118
5. **Datetime 特征提取**：year、month、week、day
6. **输出列顺序**：`date, brand_id, item_id, sales, promotion, year, month, week, day`

### 2.4 预期产物

- 行数：依实际数据确认
- 列数：9（含 promotion）

### 2.5 验证检查项

- [ ] `df.isnull().sum()` 全部为 0
- [ ] `df['sales'].min() >= 0`
- [ ] `promotion` 值域 ∈ {0, 1}
- [ ] datetime 特征值域合理

---

## 3. D3 — Rossmann Store Sales

### 3.1 数据源

```
路径：<你自己粘贴>
原始文件：Dataset3-Rossmann.csv
```

### 3.2 原始结构

| 字段 | 类型 | 说明 |
|------|------|------|
| Date | string | 日期 |
| Store | int | 门店 ID |
| Sales | int | 日销售额 |
| Customers | int | 顾客数 |
| Open | binary | 是否营业 |
| Promo | binary | 促销标识 |
| StateHoliday | categorical | 州节假日 |
| SchoolHoliday | binary | 学校假期 |
| StoreType | categorical | 门店类型 |

实体数：~1,115 stores，时间跨度约 2013-01 ~ 2015-07。

### 3.3 清洗步骤

1. **读取**：`pd.read_csv(path, parse_dates=['Date'])`
2. **列重命名**：`Date` → `date`，`Store` → `store_id`，`Sales` → `sales`
3. **保留额外特征**：`Customers` → `customers`，`Open` → `open`，`Promo` → `promo`，`StateHoliday` → `state_holiday`，`SchoolHoliday` → `school_holiday`，`StoreType` → `store_type`
4. **StateHoliday 编码**：原始值包含 '0'、'a'、'b'、'c'，转为数值编码（0/1/2/3）或 one-hot
5. **Open=0 的处理**：保留这些行（门店关门日 sales=0 是真实数据，不是缺失）
6. **数据质量检查**：
   - 确认无缺失值（或记录缺失位置）
   - `sales` 应 ≥ 0
7. **Datetime 特征提取**：year、month、week、day
8. **输出列顺序**：`date, store_id, sales, customers, open, promo, state_holiday, school_holiday, store_type, year, month, week, day`

### 3.4 预期产物

- 行数：约 1,017,209
- 列数：13
- D3 是 D1-D3 中特征最丰富的数据集（对齐论文"as many as 8 features for the 3rd dataset"）

### 3.5 验证检查项

- [ ] `df.isnull().sum()` — 记录各列缺失数
- [ ] `df['sales'].min() >= 0`
- [ ] `state_holiday` 编码后值域正确
- [ ] `open` 和 `promo` 值域 ∈ {0, 1}
- [ ] datetime 特征值域合理

---

## 4. D4 — 叮咚买菜 FreshRetailNet-LT

### 4.1 数据源

```
路径：<你自己粘贴>
原始文件：HuggingFace 下载的主表
```

### 4.2 原始结构

主键：`city_id × store_id × product_id × dt`

| 字段 | 类型 | 说明 |
|------|------|------|
| city_id | int | 城市 ID（18 个） |
| store_id | int | 门店 ID（1,057 个） |
| product_id | int | 商品 ID（576 个） |
| dt | string | 日期 |
| sale_amount | float | 日销量（0~83.7，均值 1.69） |
| management_group_id | int | 品类管理组（2 个值） |
| first/second/third_category_id | int | 三级品类体系 |
| stock_hour_6_22_cnt | int | 营业时段有货小时数（0~16） |
| activity_flag | boolean | 促销活动标识（17.4%） |
| discount | float | 折扣率（0.668~1.0） |
| holiday_flag | boolean | 节假日标识 |
| precpt | float | 降水量 mm |
| avg_temperature | float | 平均温度 |
| avg_humidity | float | 平均湿度 |
| avg_wind_level | float | 风力等级 |
| hours_sale | list[float] | 24 小时粒度销量列表 |
| hours_stock_status | list[int] | 24 小时库存状态列表 |

实体数：22,939，日期范围：2023-06-05 ~ 2025-07-13（770 天），零值率 5.47%，无负值，无缺失。

### 4.3 清洗步骤

1. **读取**：chunked 读取（文件较大），注意 `hours_sale` 和 `hours_stock_status` 是 list 类型（可能存储为 JSON string）

2. **列重命名**：`dt` → `date`，`sale_amount` → `sales`

3. **list 列展开**：将 `hours_sale` 和 `hours_stock_status` 各展开为 3 个统计量

   ```python
   # hours_sale → 3 个特征
   df['hours_sale_sum'] = df['hours_sale'].apply(lambda x: sum(x))
   df['hours_sale_max'] = df['hours_sale'].apply(lambda x: max(x))
   df['hours_sale_nonzero_hours'] = df['hours_sale'].apply(lambda x: sum(1 for v in x if v > 0))
   
   # hours_stock_status → 3 个特征
   df['hours_stock_sum'] = df['hours_stock_status'].apply(lambda x: sum(x))
   df['hours_stock_max'] = df['hours_stock_status'].apply(lambda x: max(x))
   df['hours_stock_nonzero_hours'] = df['hours_stock_status'].apply(lambda x: sum(1 for v in x if v > 0))
   ```

4. **数据完整性校验**：`hours_sale_sum` 应与 `sales` 高度一致（Pearson r > 0.99），若不一致需排查

5. **删除原始 list 列**：展开后删除 `hours_sale` 和 `hours_stock_status`（CSV 无法存储 list）

6. **city_id 保留**：不删除（实际 18 个城市，之前扫描误判为常数列是因为只扫了单店子集）

7. **boolean 列转 int**：`activity_flag`、`holiday_flag` 转为 0/1 int

8. **Datetime 特征提取**：year、month、week、day

9. **数据质量检查**：
   - 无缺失、无负值（已知）
   - 实体数 = 22,939
   - 日期范围 = 770 天

### 4.4 输出列清单

```
date, city_id, store_id, product_id, sales,
management_group_id, first_category_id, second_category_id, third_category_id,
stock_hour_6_22_cnt, activity_flag, discount, holiday_flag,
precpt, avg_temperature, avg_humidity, avg_wind_level,
hours_sale_sum, hours_sale_max, hours_sale_nonzero_hours,
hours_stock_sum, hours_stock_max, hours_stock_nonzero_hours,
year, month, week, day
```

### 4.5 预期产物

- 行数：原始行数不变（约 770 × 22,939 的稀疏子集）
- 列数：27（原 19 - 2 个 list 列 + 6 个展开统计 + 4 个 datetime）
- D4 清洗最简单——数据质量最干净，主要工作是 list 列展开

### 4.6 验证检查项

- [ ] `df.isnull().sum()` 全部为 0
- [ ] `df['sales'].min() >= 0`
- [ ] `hours_sale_sum` 与 `sales` 的 Pearson r > 0.99
- [ ] `df.groupby(['city_id','store_id','product_id']).ngroups == 22939`
- [ ] `df['city_id'].nunique() == 18`
- [ ] 无 list 类型列残留

---

## 5. D5 — Corporación Favorita

### 5.1 数据源

```
路径：<你自己粘贴>
原始文件：
  - train.csv          ← 主表（销量）
  - items.csv          ← 商品元数据
  - stores.csv         ← 门店元数据
  - transactions.csv   ← 日交易笔数
  - oil.csv            ← 油价
  - holidays_events.csv ← 节假日
```

### 5.2 原始结构

主表（train.csv）主键：`store_nbr × item_nbr × date`

| 字段 | 类型 | 说明 |
|------|------|------|
| date | string | 日期 |
| store_nbr | int | 门店（54 个） |
| item_nbr | int | 商品（4,036 个） |
| unit_sales | float | 日销量（含负值） |
| onpromotion | binary | 是否促销 |

实体数：174,685，日期范围：2013-01-01 ~ 2017-08-15（1,688 天）。

关键特殊性：train.csv **不记录零销量行**（零值率表面上是 0%，实际是因为缺失行未被记录）。

### 5.3 清洗步骤

**Step 1：读取主表**

```python
df = pd.read_csv('train.csv', parse_dates=['date'])
```

**Step 2：负值处理**

```python
df['unit_sales'] = df['unit_sales'].clip(lower=0)
```

理由：负值行仅占 0.006%（7,795 行），但涉及 63.6% 的 item，排除代价太大。负值中位数为 -1，绝大多数是小额退货冲销。

特别标记两个极端 item（供后续敏感性实验）：item 759657（min=-1,049）和 item 1464093（min=-456）。

**Step 3：Join 辅助表**

按以下顺序 join（顺序有依赖关系）：

| 序号 | 表 | Join Key | 新增特征 | 注意事项 |
|------|-----|----------|----------|----------|
| ① | items.csv | item_nbr | family, class, perishable | 100% 覆盖 |
| ② | stores.csv | store_nbr | city, state, type, cluster | 100% 覆盖 |
| ③ | oil.csv | date | oil_price | 见 Step 4 单独处理 |
| ④ | transactions.csv | store_nbr × date | transactions | ~99% 覆盖，缺失填 0 |
| ⑤ | holidays_events.csv | date（+ locale 条件） | is_holiday | 见 Step 5 单独处理 |

**Step 4：Oil 价格处理（严格顺序，防止 leakage）**

```python
oil = pd.read_csv('oil.csv', parse_dates=['date'])
oil = oil.sort_values('date')
oil['dcoilwtico'] = oil['dcoilwtico'].ffill()      # 1) ffill 填充周末缺失
oil['oil_price'] = oil['dcoilwtico'].shift(1)       # 2) shift(1) 滞后 1 天
oil = oil[['date', 'oil_price']]
df = df.merge(oil, on='date', how='left')
df['oil_price'] = df['oil_price'].ffill().bfill()   # 处理首日 NaN
```

操作顺序 `ffill → shift(1) → join` 是硬性要求，颠倒会导致未来信息泄漏。

**Step 5：Holidays 处理**

```python
holidays = pd.read_csv('holidays_events.csv', parse_dates=['date'])
```

holidays_events.csv 存在一对多问题（同一天可能有 National + Regional 事件）。处理规则：

- locale 优先级：National > Regional > Local
- 同一天取最高优先级
- 最终转为二值列 `is_holiday ∈ {0, 1}`

**Step 6：零值行补全**

这是 D5 最关键的清洗步骤。train.csv 不记录零销量，必须显式补全：

```python
# 构造完整的 store × item × date 笛卡尔积
all_stores = df['store_nbr'].unique()
all_items = df['item_nbr'].unique()
all_dates = pd.date_range(df['date'].min(), df['date'].max(), freq='D')

full_index = pd.MultiIndex.from_product(
    [all_stores, all_items, all_dates],
    names=['store_nbr', 'item_nbr', 'date']
)

df = df.set_index(['store_nbr', 'item_nbr', 'date']).reindex(full_index).reset_index()
df['sales'] = df['unit_sales'].fillna(0)  # 补全的行 sales=0
```

注意：补全后行数会极大膨胀（54 × 4,036 × 1,688 ≈ 3.68 亿行）。必须使用 chunked 或 dask 处理。补全后辅助特征列（family、city 等元数据）需通过 store/item 再次 ffill 或 re-merge 补回。

**Step 7：列重命名**

`unit_sales` → `sales`（如果 Step 6 未完成重命名）

**Step 8：Datetime 特征提取**

year、month、week、day

**Step 9：onpromotion 缺失处理**

补全行的 `onpromotion` 填 0（缺失日视为无促销）。

### 5.4 输出列清单

```
date, store_nbr, item_nbr, sales, onpromotion,
family, class, perishable,
city, state, type, cluster,
transactions, oil_price, is_holiday,
year, month, week, day
```

### 5.5 预期产物

- 行数：补全后最大 ~3.68 亿行（实际取决于 item 首次出现日期——仅补全 item 在数据集中存在期间的缺失行）
- 列数：19
- 这是六个数据集中清洗最复杂的一个

### 5.6 内存与性能注意

- 补零操作的笛卡尔积可能超出内存，建议按 store 分组处理后 concat
- 或使用 dask：`dask.dataframe` 延迟计算
- oil join 和 holidays join 可以在补零前（只对已有行）或补零后（对全量行）做，效果相同但补零前效率更高
- 最终 CSV 可能 >10GB，考虑是否需要 gzip 压缩（`df.to_csv('D5_clean.csv.gz', compression='gzip')`）

### 5.7 验证检查项

- [ ] `df['sales'].min() >= 0`（负值已 clip）
- [ ] `df.isnull().sum()` — 除补全行外无缺失
- [ ] `oil_price` 无 NaN
- [ ] `is_holiday` 值域 ∈ {0, 1}
- [ ] `onpromotion` 值域 ∈ {0, 1}，无 NaN
- [ ] 补零前后行数差 = 补全的零销量行数
- [ ] `transactions` 列：原始存在的行有值，补全行填 0
- [ ] 抽查：选一个 store×item，确认其日期序列连续且零销量日已补全

### 5.8 跨数据集注意

补零会导致 D5 的 RMSE 和 sMAPE 与 D4/D6 不直接可比（大量零值行影响指标），这一点需要在论文数据集描述中说明。sMAPE 中的 epsilon 对补零后数据尤其关键。

---

## 6. D6 — M5 Forecasting (Walmart)

### 6.1 数据源

```
路径：<你自己粘贴>
原始文件：
  - sales_train_evaluation.csv  ← 主表（宽表，d_1 ~ d_1941）
  - calendar.csv                ← 日期映射 + 事件 + SNAP
  - sell_prices.csv             ← 周度价格
```

### 6.2 原始结构

主表为宽表格式：每行一个 item×store，列 d_1 ~ d_1941 为每日销量。

| 字段 | 类型 | 说明 |
|------|------|------|
| item_id | string | 商品 ID（3,049 个） |
| store_id | string | 门店 ID（10 个：CA_1~4, TX_1~3, WI_1~3） |
| dept_id | string | 部门（7 个） |
| cat_id | string | 大类（3 个：FOODS, HOBBIES, HOUSEHOLD） |
| state_id | string | 州（3 个：CA, TX, WI） |
| d_1 ~ d_1941 | int | 每日销量 |

实体数：30,490（3,049 items × 10 stores），日期范围：2011-01-29 ~ 2016-06-19（1,941 天），零值率 68%，无负值，无缺失。

### 6.3 清洗步骤

**Step 1：宽表转长表**

这是 D6 最耗资源的操作（30,490 行 × 1,941 列 → ~5,920 万行）。

```python
id_cols = ['item_id', 'store_id', 'dept_id', 'cat_id', 'state_id']
df = pd.melt(
    wide_df,
    id_vars=id_cols,
    value_vars=[f'd_{i}' for i in range(1, 1942)],
    var_name='d',
    value_name='sales'
)
```

内存优化：可分 chunk（如每次 melt 5,000 行 item×store）后 concat，或用 dask。

**Step 2：Join calendar.csv（日期映射 + 事件 + SNAP）**

```python
calendar = pd.read_csv('calendar.csv')
df = df.merge(calendar[['d', 'date', 'wm_yr_wk', 'weekday', 'wday', 'month', 'year',
                          'event_name_1', 'event_type_1', 'event_name_2',
                          'snap_CA', 'snap_TX', 'snap_WI']],
              on='d', how='left')
df['date'] = pd.to_datetime(df['date'])
```

**Step 3：事件列转换**

```python
df['is_event_1'] = df['event_name_1'].notna().astype(int)   # 91.8% 为 0
df['is_event_2'] = df['event_name_2'].notna().astype(int)   # 99.7% 为 0

# event_type_1 保留 4 类编码（Sporting, Cultural, National, Religious），缺失填 'None'
df['event_type_1'] = df['event_type_1'].fillna('None')
```

删除原始 `event_name_1`、`event_name_2` 列（文本列不参与建模）。

**Step 4：SNAP 列合并**

三个州各自有 SNAP 列，按实体所属州动态选取：

```python
snap_map = {'CA': 'snap_CA', 'TX': 'snap_TX', 'WI': 'snap_WI'}
df['snap'] = df.apply(lambda row: row[snap_map[row['state_id']]], axis=1)
```

删除原始 `snap_CA`、`snap_TX`、`snap_WI` 列。

性能优化：避免 `apply`，用 numpy `where` 或 `np.select` 向量化。

**Step 5：Join sell_prices.csv**

```python
prices = pd.read_csv('sell_prices.csv')
df = df.merge(prices, on=['store_id', 'item_id', 'wm_yr_wk'], how='left')
```

覆盖率 100%。注意 `sell_price` 是周度粒度（同一周内所有天共享同一价格），这是数据本身的设计，不需要插值。

**Step 6：Datetime 特征提取**

calendar.csv 已包含 `weekday`、`wday`、`month`、`year`，但缺少 `week` 和 `day`：

```python
df['week'] = df['date'].dt.isocalendar().week.astype(int)
df['day'] = df['date'].dt.day
```

保留 calendar 已有的 `month`、`year`（无需重复提取）。

**Step 7：删除中间列**

删除 `d`（d_1 格式标识列，已 join date 后不再需要）和 `wm_yr_wk`（已 join 价格后不再需要）。

**Step 8：68% 零值率——不做任何处理**

这是 M5 的结构性特征（长尾商品），保留原样。论文中明确说明。

### 6.4 输出列清单

```
date, item_id, store_id, dept_id, cat_id, state_id, sales,
sell_price, weekday, wday,
is_event_1, is_event_2, event_type_1, snap,
year, month, week, day
```

### 6.5 预期产物

- 行数：~59,200,000（30,490 × 1,941）
- 列数：18
- CSV 可能 >3GB，考虑 gzip 压缩

### 6.6 event_type_1 编码说明

`event_type_1` 当前保留为字符串分类列（Sporting / Cultural / National / Religious / None）。如需数值编码，放到实验 pipeline 中做 label encoding 或 one-hot，不在此步骤处理。

### 6.7 验证检查项

- [ ] 转长表后行数 = 30,490 × 1,941 = 59,181,090
- [ ] `df['sales'].min() >= 0`，`df['sales'].dtype` 为 int
- [ ] `df.isnull().sum()` — sell_price 无 NaN
- [ ] `is_event_1` 和 `is_event_2` 值域 ∈ {0, 1}
- [ ] `snap` 值域 ∈ {0, 1}
- [ ] `event_type_1` 值域 ∈ {'Sporting', 'Cultural', 'National', 'Religious', 'None'}
- [ ] `df.groupby(['item_id','store_id']).ngroups == 30490`
- [ ] 日期范围 = 2011-01-29 ~ 2016-06-19

---

## 7. 执行顺序与依赖

```
D1 ──┐
D2 ──┤  无依赖，可并行
D3 ──┤
D4 ──┤
D5 ──┤  ← 最耗时（补零 + 多表 join），建议优先启动
D6 ──┘  ← 次耗时（宽转长）
```

建议执行顺序：先跑 D5 和 D6（耗时最长），同时并行跑 D1-D4（快速完成）。

每个数据集清洗完成后，立即运行对应的验证检查项，确认通过后再进入下一步（目标域/源域分离）。

---

## 8. 下游衔接

本步骤产出的 `Dx_clean.csv` 是下游分离脚本的唯一输入。分离脚本将：

1. 根据 target selection 结果，按实体维度拆分为 `Dx_target.csv` + `Dx_source.csv`
2. 对 D4-D6 的源域做分层抽样
3. 不做时间窗口裁剪（留给实验 pipeline）

分离方案将在独立的 MD 文档中定义。
